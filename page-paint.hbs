{{!< default}}

{{!-- This is a page template. A page outputs content just like any other post, and has all the same
    attributes by default, but you can also customise it to behave differently if you prefer. --}}

{{!-- Everything inside the #post tags pulls data from the page --}}
{{#post}}

<header class="main-header post-head {{#if image}}" style="background-image: url({{image}}){{else}}no-cover{{/if}}">
    <nav class="main-nav overlay clearfix">
        {{#if @blog.logo}}<a class="blog-logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}}" /></a>{{/if}}
        {{#if @blog.navigation}}
            <a class="menu-button icon-menu" href="#"><span class="word">Menu</span></a>
        {{/if}}
    </nav>

    <div class="main-header-content">
        <h1 class="page-title">{{@blog.title}}</h1>
        <h2 class="page-description">{{@blog.description}}</h2>
    </div>
</header>

<main class="content" role="main">
    <article class="{{post_class}}">

        <header class="post-header">
            <h1 class="post-title">{{title}}</h1>
        </header>

        <div class="help_box">Paint a picture about the Olympics. It doesn't have to be any good, as long as you enjoy making it.</div>

        <section class="post-content">
          <div class="imp-paint"></div>
          <div class="paintDescription">"Ecce Homo", by Badly-Drawing Boy, aged 100</div>
            {{content}}

              <form id="paintForm" name="paintForm">
                <label class="nameLabel">Title of your painting<input type="text" size="32" maxlength="32" id="paintTitle" name="paintTitle" placeholder="Ecce Homo"/></label>
                <label class="nameLabel">Your name (public)<input type="text" size="32" maxlength="32" id="paintAuthor" name="paintAuthor" placeholder="Badly-Drawing Boy"/></label>
                <label class="nameLabel">Your age<input type="number" min="0" max="100" id="paintAge" name="paintAge" value="100"/></label>
                <a href="/" class="returnLink">< Back to homepage</a>
                <button type="button" onclick="return impromptu.submitPainting()">Submit</button>
                <div class="submitNote">You can't edit the image after you submit</div>
                <div class="error"</div>
              </form>
        </section>

    </article>
</main>
{{/post}}
